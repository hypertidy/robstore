% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{store_head_bytes}
\alias{store_head_bytes}
\title{Read the same byte range from many objects concurrently}
\usage{
store_head_bytes(store, keys, length, offset = 0, concurrency = 16)
}
\arguments{
\item{store}{A \code{Store} object.}

\item{keys}{Character vector of object keys.}

\item{length}{Single numeric length applied to every key.}

\item{offset}{Single numeric offset applied to every key (default \code{0}).}

\item{concurrency}{Maximum number of concurrent requests.}
}
\value{
A list of raw vectors in input order.
}
\description{
Convenience wrapper around \code{\link[=store_get_ranges_many]{store_get_ranges_many()}} for the common case
of fetching the first N bytes of each file in a batch — e.g. COG headers,
the start of a Parquet file, or a fixed Zarr chunk slot.
}

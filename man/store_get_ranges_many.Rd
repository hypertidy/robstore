% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{store_get_ranges_many}
\alias{store_get_ranges_many}
\title{Read many byte ranges concurrently.}
\usage{
store_get_ranges_many(store, keys, offsets, lengths, concurrency)
}
\arguments{
\item{store}{A \code{Store} object.}

\item{keys}{Character vector of object keys.}

\item{offsets}{Numeric vector of 0-based byte offsets (same length).}

\item{lengths}{Numeric vector of byte lengths (same length).}

\item{concurrency}{Maximum number of concurrent requests.}
}
\value{
A list of raw vectors.
}
\description{
Each of the three input vectors \code{keys}, \code{offsets}, \code{lengths} must have
the same length. Issues up to \code{concurrency} range-GET requests at once.
Returns a list of raw vectors in input order. Ranges can target the
same key or different keys — this is the primitive for concurrent
COG IFD walks, Zarr chunk reads, Parquet row-group reads.
}
